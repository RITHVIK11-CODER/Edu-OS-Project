# EduOS Web

Web client for the EduOS student MVP.

## Development

This directory is intentionally separate from `apps/mobile/`. It will use the existing FastAPI backend contracts and keep mock services as a fallback during hackathon development.

## MVP flow

Login → Dashboard → Mathematics → Topic → SmartAssess → Question → AI Analysis → Recommendation → Practice → Reassessment → Progress
