import {supabase} from './supabase';
const API_BASE_URL=import.meta.env.VITE_API_BASE_URL||'http://localhost:8000/api/v1';
export async function request<T>(path:string,options:RequestInit={}):Promise<T>{const{data:{session}}=await supabase.auth.getSession();const headers=new Headers(options.headers);headers.set('Content-Type','application/json');headers.set('Accept','application/json');headers.set('X-Request-ID',crypto.randomUUID());if(session?.access_token)headers.set('Authorization','Bearer '+session.access_token);const res=await fetch(API_BASE_URL+path,{...options,headers});const body=await res.json().catch(()=>({}));if(!res.ok||body.success===false)throw new Error(body?.error?.message||body?.detail||'Request failed ('+res.status+')');return(body.data??body)as T;}
export const api={me:()=>request<any>('/auth/me'),student:()=>request<any>('/students/me'),twin:()=>request<any>('/students/me/learning-twin'),assessments:()=>request<any[]>('/assessments'),start:(id:string)=>request<any>('/assessments/'+id+'/start',{method:'POST'}),questions:(id:string)=>request<any[]>('/assessments/'+id+'/questions'),answer:(id:string,body:any)=>request<any>('/attempts/'+id+'/answers',{method:'POST',body:JSON.stringify(body)}),submit:(id:string)=>request<any>('/attempts/'+id+'/submit',{method:'POST'}),result:(id:string)=>request<any>('/attempts/'+id+'/result'),mindtrace:(answerId:string)=>request<any>('/mindtrace/analyze',{method:'POST',body:JSON.stringify({answer_id:answerId})}),pathai:(conceptId:string)=>request<any>('/pathai/recommend',{method:'POST',body:JSON.stringify({concept_id:conceptId})})};
